import { LoginStackScreenProps } from "@hongpung/src/navigation/LoginStackNavigation";
import { View } from "react-native";

const SignUpPage: React.FC<LoginStackScreenProps<"SignUp">> = ({ navigation }) => {
  return (
    <View>
      
    </View>
  );
};

export default SignUpPage;
